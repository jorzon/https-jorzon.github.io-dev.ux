const NAME = 'mediacontacts-app',
    DOMAIN = `http://127.0.0.1:5500/`,
    SITE = `${DOMAIN}`,
    CARDS = `${SITE}/json/cards.json`,
    DETAIL = `${SITE}/json/`

export default {
    NAME,
    DOMAIN,
    SITE,
    CARDS,
    DETAIL
}